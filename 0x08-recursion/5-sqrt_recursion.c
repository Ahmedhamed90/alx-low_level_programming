#include "main.h"

/**
 * _sqrt_recursion - a natural square root
 *
 * @n: int
 *
 * Return: int
*/

int _sqrt_recursion(int n)
{
	int x;

	if (n == x)
	{
		return (x);
	}
	else if (n < x)
	{
		return (x.sqrt(n));
	}
	else
		return (-1);
}
