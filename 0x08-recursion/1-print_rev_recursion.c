#include "main.h"

/**
 * _print_rev_recursion - pointer
 *
 * @s: pointer a string
 *
 * Return: void
*/

void _print_rev_recursion(char *s)
{
	char x = "";

	for (int z = *s.length() - 1 ; z >= 0; z--)
	{
		x += *s[z]
	}
}
